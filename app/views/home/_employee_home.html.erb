<% if current_user.employee.shifts.upcoming.first.date == Date.current %>
    <%= link_to(start_shift_path(shift_id: current_user.employee.shifts.upcoming.first.id), class: 'button radius') do %>
        &nbsp;<%= "Start Shift" %>
    <% end %>

<% end %>