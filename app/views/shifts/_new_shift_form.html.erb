<%= simple_form_for @shift, html: { :class => 'form-horizontal' }, remote: true do |f| %>
    <%= f.input :start_time, label: "Shift", input_html: {class: "avg"} %>
    
    <!-- Note in this case that can't use @project; use params[:project_id] -->
    <!--%= f.input :project_id, as: :hidden, input_html: { value: params[:project_id] } %-->

    <!--%= f.input :priority, :collection => Shift::PRIORITY_LIST, include_blank: true, input_html: {class: "avg"} %-->
    <p>&nbsp;</p>
    <div class="form-actions">
      <%= f.submit nil, class: 'button radius' %>
      <%= link_to 'Cancel', shifts_path, class: 'button radius alert' %>
    </div>
<% end %>