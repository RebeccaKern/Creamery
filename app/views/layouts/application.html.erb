<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>A &amp; M Creamery</title>
    <%= favicon_link_tag %>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="right">
    <% if logged_in? %>
    [<%= current_user.employee.first_name %> <%= current_user.employee.last_name %> : <%= current_user.employee.role %>]
    <%= link_to 'Logout', logout_path%>
  <% else %>
    <%= link_to 'Login', login_path %>
  <% end %>
  </div>

    <div id="title_area">
      <%= link_to image_tag('logo.png'), home_path %>
    </div>
    <%= render :partial => "partials/nav" %>
    
    <div class="row">
      <div class="small-12 columns" id="main_content">
        <%= render :partial => "partials/flash" %>
        <%= yield %>
      </div>
    </div>
    <footer>
      <div class="small-12 columns" id="footer">
        <%= render partial: 'partials/footer' %>
      </div>
    </footer> 
    <%= javascript_include_tag "application" %>
  </body>
</html>
